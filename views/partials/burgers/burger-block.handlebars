<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newBurger="{{devoured}}">
		{{#if devoured}}Eat it{{else}}Eremake this burger{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
