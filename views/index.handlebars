<div class="container">
  
  <h1>Heres your burger!</h1>
  
 

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers We've eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Make A Burger!!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="burg" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Save it for later or eat it now?</label><br>
    <input type="radio" name="devoured" value="1" checked> Eat it now!<br>
    <input type="radio" name="devoured" value="0"> Save for later!
  </div>

  <button class="btn btn-success" type="submit">Make this burger</button>
</form>
 </div>